# IRSA annotation on the checkout service account (dev).
# Replace with terraform output when dev environment exists.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: checkout
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::REPLACE_ACCOUNT_ID:role/shopcloud-checkout-dev
