# Placeholder Secret. Do not commit real values to git.
# In production, source these from AWS Secrets Manager via the External Secrets Operator
# or the Secrets Store CSI Driver. The keys here match what shared/config.py reads.
apiVersion: v1
kind: Secret
metadata:
  name: shopcloud-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://shopcloud:CHANGE_ME@postgres:5432/shopcloud"
  REDIS_URL: "redis://redis:6379"
